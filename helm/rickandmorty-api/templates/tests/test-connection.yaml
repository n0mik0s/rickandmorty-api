apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rickandmorty-api-helm.fullname" . }}-test-connection"
  labels:
    {{- include "rickandmorty-api-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rickandmorty-api-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
